# Scoring Rework Report

작성일: 2026-04-03

상태: 분석 완료, 구현 미착수

## 1. 목적

이 문서는 현재 HEXACO/BHI 기반 점수 계산과 최종 유형 분류 로직의 문제를 정리하고, 수정 방향을 구현 전에 합의하기 위한 기준 문서다.

핵심 목표는 다음과 같다.

- BHI 원논문에 가까운 점수 계산과 제품용 유형 분류를 분리한다.
- `균형잡힌 연구자(T9)`가 과도하게 선택되는 원인을 제거한다.
- 장면 순서와 문항 ID 매핑 불일치를 해소한다.
- 결과 해석 문구가 실제 계산 방식과 어긋나지 않도록 정리한다.

## 2. 현재 코드 기준 사실 관계

### 2.1 BHI 원형과 현재 프로젝트의 차이

현재 프로젝트는 BHI를 그대로 구현한 것이 아니라, BHI 문항을 시나리오형 선택지로 변형한 게임형 측정이다.

관련 문서:

- `BHI.md`
- `hexaco_detailed_plan_v2.md`

관련 코드:

- `src/lib/questions.ts`
- `src/lib/scoring.ts`
- `src/app/result/page.tsx`

현재 구조는 크게 두 층으로 나뉜다.

- 1층: 문항 응답 -> 요인 점수 계산
- 2층: 요인 점수 -> 제품용 대학원생 유형(T1~T9) 분류

문제는 이 두 층이 현재 코드와 문서에서 충분히 분리되어 있지 않다는 점이다.

### 2.2 현재 점수 계산 로직

현재 구현된 요인 점수 계산은 아래와 같다.

1. 각 문항 응답을 1~5로 저장
2. 결과 계산 시 같은 요인 4문항 평균을 구함
3. `(mean - 1) / 4 * 100`으로 0~100% 정규화

이 부분은 BHI 기반 간이 점수로 볼 수 있다.

현재 구현 위치:

- `src/lib/scoring.ts`

현재 결과 분류 로직은 아래와 같다.

1. 6개 요인 점수의 표준편차 `profileSD` 계산
2. `profileSD < 0.8165` 이면 즉시 `T9 = 균형잡힌 연구자`
3. 그렇지 않으면 T1~T8 가중치 매칭 점수 계산

문제의 핵심은 2번이다.

## 3. 핵심 문제

### 3.1 문제 A: T9가 다른 타입을 너무 많이 선점함

현재 `src/lib/scoring.ts`에는 아래와 같은 구조가 있다.

```ts
const BALANCED_THRESHOLD = 4 / (2 * Math.sqrt(6)); // ≈ 0.8165

if (profileSD < BALANCED_THRESHOLD) {
  return T9;
}
```

이 규칙은 BHI 원논문 공식이 아니다. 프로젝트 문서에서 후처리 규칙으로 추가된 제품 로직이다.

문제는 이 기준값이 실제 사용자 응답 분포에 비해 너무 크다는 점이다.

로컬 시뮬레이션 결과:

- 균등 무작위 응답에서도 약 85%가 T9
- 가운데 값 선호가 있는 응답 분포에서는 약 98%가 T9

즉 현재 구조에서는 다른 타입이 약해서 T9가 나오는 것이 아니라, T9 판정이 지나치게 먼저 발동해서 다른 타입 계산이 사실상 무의미해진다.

예시:

- `H=4, C=4, X=2, A=3, E=3, O=3`
- 이 프로필은 T1 성향이 충분히 보이지만 `profileSD=0.687`로 계산되어 현재 로직에서는 T9로 고정된다.

결론:

- 현재 T9는 "균형형"이 아니라 "중간값이 많은 프로필"을 과도하게 빨아들이는 하드 오버라이드다.

### 3.2 문제 B: BHI 점수 계산과 제품용 분류가 뒤섞여 있음

BHI 원논문에서 직접 제공하는 것은 문항 채점과 요인 수준 해석이다. 현재 프로젝트의 T1~T9 유형은 제품용 2차 가공 결과다.

하지만 현재 문서와 결과 화면은 이 둘을 명확히 구분하지 않는다.

문제점:

- 사용자는 지금 결과를 마치 BHI 원검사에서 나온 공식 유형처럼 받아들일 수 있다.
- 실제로는 요인 점수까지만이 BHI 기반이고, 유형은 별도 설계물이다.
- 따라서 "논문과 똑같이 계산했다"는 식의 해석은 현재 구조상 맞지 않다.

정리:

- BHI 기반 점수 계산은 유지 가능
- T1~T9는 제품용 분류 체계로 별도 명시해야 함

### 3.3 문제 C: 장면 순서와 문항 ID 매핑이 일부 에피소드에서 어긋남

현재 저장은 `EpisodeClient`에서 "현재 질문 index"에 해당하는 `question.id`에 scene 선택값을 기록하는 방식이다.

관련 코드:

- `src/app/test/[episode]/EpisodeClient.tsx`

문제는 각 scene 컴포넌트가 자체 `const QUESTIONS` 배열을 따로 가지고 있고, 이 배열 순서가 `src/lib/questions.ts`의 문항 순서와 일부 에피소드에서 맞지 않는다는 점이다.

확인된 불일치:

- EP1 OT
  - 문서상 매핑: `x1=대화`, `x2=좌석`, `x3=자기소개`, `x4=뒤풀이`
  - 실제 UI 순서: `좌석 -> 자기소개 -> 뒤풀이 -> 대화`
- EP2 Desk
  - 문서상 매핑: `c1=과제 대응`, `c2=마감 vs 친구`, `c3=모르는 용어`, `c4=제출 전 검토`
  - 실제 UI 순서: `과제 대응 -> 모르는 용어 -> 마감 vs 친구 -> 제출 전 검토`
- EP3 Lab
  - 문서상 매핑: `a1=아이디어 도용`, `a2=후배 실수`, `a3=분석 충돌`, `a4=선배 부탁`
  - 실제 UI 순서: `선배 부탁 -> 분석 충돌 -> 후배 실수 -> 아이디어 도용`
- EP4 Conference
  - 문서상 매핑: `h1=교수 관심`, `h2=애매한 데이터`, `h3=연구비 자랑`, `h4=추켜세움`
  - 실제 UI 순서: `애매한 데이터 -> 교수 관심 -> 연구비 자랑 -> 추켜세움`
- EP5 Burnout
  - 현재 순서 일치
- EP6 Graduation
  - 현재 순서 일치

영향:

- 요인 평균 자체는 같은 에피소드 내 4문항 평균이라 당장 큰 오차가 드러나지 않을 수 있다.
- 하지만 item-level 저장, 역문항 정합성, 문서-코드 일치성은 깨진다.
- 이후 문항별 분석이나 품질 검증 시 데이터 해석이 틀어진다.

### 3.4 문제 D: "높은 편 / 보통 / 낮은 편" 구간은 규준이 아니라 단순 선형 분할

결과 화면에서 요인 막대는 아래 규칙으로 라벨링된다.

- 66.7 이상 = 높은 편
- 33.3 이상 = 보통
- 그 미만 = 낮은 편

관련 코드:

- `src/app/result/page.tsx`

이 구간은 모집단 규준이나 백분위가 아니라 0~100 선형 값의 3등분이다.

즉 현재 라벨은 아래 의미만 가진다.

- 응답 스케일 내부에서 상대적으로 높다/중간이다/낮다

아래 의미는 가지지 않는다.

- 실제 사람들 평균보다 높다
- 논문 규준상 높다

따라서 이 라벨은 UI 표현으로는 가능하지만, "심리측정적 높고 낮음"처럼 보이게 쓰면 안 된다.

## 4. 원칙 정리

이번 수정은 아래 원칙을 따라야 한다.

### 4.1 원칙 1: BHI 기반 계산과 제품용 분류를 분리한다

분리 기준:

- BHI 기반 계산
  - 문항 응답
  - 역채점
  - 요인별 평균
- 제품용 분류
  - T1~T9
  - 유사 유형
  - 균형 성향 판단

이 분리가 되어야 문서, 코드, 결과 해석이 일관된다.

### 4.2 원칙 2: T9는 하드 오버라이드가 아니라 보조 판단으로 다룬다

`균형잡힌 연구자`는 "모든 다른 타입보다 먼저 먹는 기본 결과"가 아니라, "프로필이 얼마나 평평한가"를 설명하는 보조 지표로 보는 쪽이 타당하다.

즉 다음 중 하나로 바꾸는 것이 적절하다.

- 옵션 A: T9를 제거하고 균형도만 별도 표시
- 옵션 B: T1~T8은 항상 계산하고, 균형 성향을 배지로 함께 표시
- 옵션 C: 매우 엄격한 조건일 때만 T9를 단독 결과로 허용

권장:

- 기본 권장안은 옵션 B
- 제품적으로 T9를 꼭 유지해야 하면 옵션 C를 추가

### 4.3 원칙 3: 장면과 문항의 연결은 순서가 아니라 명시적 ID로 묶는다

현재처럼 각 scene가 독립 배열을 가지고 "암묵적으로 질문 순서가 맞겠지"라고 가정하면 다시 같은 문제가 난다.

따라서 scene 데이터는 최소한 아래 속성을 가져야 한다.

- `questionId`
- `factor`
- `prompt`
- `choices`

이렇게 해야:

- 문서-코드-저장값이 일치한다
- 역문항 검증이 가능해진다
- UI 순서를 바꿔도 저장이 틀어지지 않는다

## 5. 권장 수정 방향

### 5.1 권장안 요약

이번 리워크의 권장 방향은 아래와 같다.

1. BHI 점수 계산을 정석대로 복원
2. 유형 분류를 별도 레이어로 분리
3. T9 하드 오버라이드 제거
4. scene-question 매핑을 `questionId` 기준으로 재구성
5. 결과 화면에서 "요인 점수"와 "제품용 유형"을 분리 표현

### 5.2 점수 계산 레이어

점수 계산 레이어는 아래 책임만 가진다.

- 응답값 취합
- 역채점 적용
- 요인 평균 계산
- 선택적으로 0~100 선형 스케일 변환

여기서는 제품용 유형을 계산하지 않는다.

### 5.3 유형 분류 레이어

유형 분류 레이어는 아래 책임만 가진다.

- T1~T8 가중치 매칭 점수 계산
- 1위/2위 유형 선택
- top1-top2 간격 계산
- 균형도 계산
- 필요시 균형형 단독 결과 여부 판단

권장 기본 규칙:

- T1~T8은 항상 계산한다.
- `balance`는 별도 수치 또는 배지로 보여준다.
- `T9`는 아래 조건이 동시에 만족될 때만 단독 결과 후보가 된다.
  - profileSD가 매우 낮음
  - top1-top2 격차도 매우 작음

이렇게 해야 "평평한데도 특정 성향이 조금 더 강한 사람"을 T9로 덮어버리지 않는다.

### 5.4 T9 처리 대안

#### 대안 1: T9 제거

장점:

- 가장 명확하다.
- 오분류 가능성이 줄어든다.

단점:

- 기획상 9번째 타입이 사라진다.

#### 대안 2: T9를 "균형 성향" 배지로 변경

예시:

- 주유형: 전략적 생존자
- 보조 성향: 균형도가 높은 편

장점:

- 해석이 가장 자연스럽다.
- 현재 문제를 가장 안전하게 해결한다.

단점:

- 기존 카드형 결과 UX를 조금 수정해야 한다.

#### 대안 3: T9 유지, 조건 강화

예시 조건:

- `profileSD`가 매우 낮음
- `top1 - top2` 차이도 매우 작음

또는:

- 실제 수집 데이터 기준 하위 10~20% 분화도에만 허용

장점:

- T9를 유지할 수 있다.

단점:

- 기준 설정이 필요하다.
- 데이터 없이 고정 cutoff를 두면 다시 같은 문제가 생길 수 있다.

현재 권장 결론:

- 우선 구현은 대안 2
- 실제 응답 데이터가 쌓이면 대안 3 재검토

## 6. 구현 범위 제안

아래는 구현 시 손대야 할 파일 범위다.

### 6.1 필수 수정 파일

- `src/lib/scoring.ts`
- `src/lib/questions.ts`
- `src/app/result/page.tsx`
- `src/app/test/[episode]/EpisodeClient.tsx`
- `src/components/episodes/OTScene.tsx`
- `src/components/episodes/DeskScene.tsx`
- `src/components/episodes/LabScene.tsx`
- `src/components/episodes/ConferenceScene.tsx`

### 6.2 선택적 구조 개선

아래와 같은 별도 데이터 파일을 두는 것이 바람직하다.

- `src/lib/sceneQuestions.ts`

여기에 각 장면 질문을 `questionId`와 함께 정의하면 scene 컴포넌트는 렌더링만 담당하고, 채점 기준은 단일 소스에서 관리할 수 있다.

## 7. 구현 전 합의해야 할 사항

구현 전에 아래 두 가지를 확정해야 한다.

### 7.1 T9를 유지할지

선택지:

- 유지하지 않음
- 보조 배지로 유지
- 단독 유형으로 유지하되 조건 강화

권장:

- 보조 배지로 유지

### 7.2 "높은 편/보통/낮은 편" 문구를 어떻게 쓸지

선택지:

- 현재처럼 단순 구간 라벨 유지
- 문구를 더 약하게 변경
  - 예: "상대적으로 높음", "중간", "상대적으로 낮음"
- 규준 확보 전까지 라벨 제거

권장:

- 문구를 더 약하게 바꾸는 편이 안전하다.

## 8. 구현 체크리스트

이번 구현에서 반드시 만족해야 할 체크리스트:

- BHI 점수 계산과 유형 분류 함수 분리
- T9 short-circuit 제거
- scene별 질문에 `questionId` 명시
- 문서 매핑과 코드 매핑 일치
- 결과 화면에서 주유형과 균형도를 분리 표현
- item-level 저장값이 실제 scene 의미와 일치

## 9. 검증 계획

구현 후 아래 검증이 필요하다.

### 9.1 단위 검증

- 역문항 8개가 의도대로 처리되는지
- 요인 평균이 기대값과 일치하는지
- 동일한 응답 세트에 대해 결과가 재현되는지

### 9.2 시나리오 검증

아래 프로필 예시로 결과를 확인한다.

- 모든 요인이 거의 비슷한 프로필
- T1 성향이 분명한 프로필
- T2 성향이 분명한 프로필
- H/C는 높고 X는 낮은 혼합형 프로필
- 가운데 응답이 많은 프로필

검증 포인트:

- 현재처럼 대부분 T9로 쏠리지 않는지
- 강한 프로필이 실제로 해당 타입으로 나오는지
- 균형도 표현이 주유형을 덮어쓰지 않는지

### 9.3 UI 검증

- 결과 화면 문구가 계산 방식과 맞는지
- "유사 유형"과 "균형 성향"이 혼동되지 않는지
- 막대 라벨이 과장된 해석으로 읽히지 않는지

## 10. 최종 권고

이번 리워크는 단순 threshold 조정으로 끝내면 안 된다.

현재 문제의 본질은 다음 세 가지다.

- T9를 하드 오버라이드로 둔 설계 오류
- BHI 점수와 제품용 유형을 혼동한 설명 구조
- scene와 question ID의 암묵적 순서 의존

따라서 권장하는 수정 방향은 아래 한 줄로 요약된다.

`BHI 기반 요인 점수 계산은 정석대로 유지하고, 제품용 유형 분류는 별도 레이어로 분리하며, T9는 하드 오버라이드가 아니라 보조 해석으로 다룬다.`

## 11. 비고

이 문서는 구현 전 기준 문서이며, 아직 코드 변경은 수행하지 않았다.
